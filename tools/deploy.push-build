#/usr/bin/bash -v

# push all

VERSION=`cat ../../.version`
BUILD_NAME="Build $VERSION"

#"`date "+%Y-%m-%d %H:%M:%S"`"

./deploy.html-pages
./deploy.wiki

cd ../

git commit -a -m "$BUILD_NAME"

BUILD_MESSAGE=`git log --pretty=format:"%s"  --graph  --branches --not --remotes`

git push -m "$BUILD_NAME: \r\n$BUILD_MESSAGE"

cd tools/